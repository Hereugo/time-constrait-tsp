[project]
name = "time-constrained-tsp-analysis"
version = "0.1.0"
description = "Analysis scripts for Time-Constrained TSP experiment result sets."
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.9",
    "pandas>=2.2",
    "tabulate>=0.9",
]

[tool.uv]
package = false
